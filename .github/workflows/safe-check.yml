name: safe-check

on:
push:
pull_request:

jobs:
safe-check:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- name: Secret pattern scan
run: bash scripts/safe-scan.sh
- name: Install
run: npm install
- name: Build
run: npm run build
